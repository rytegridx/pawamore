# Cloudflare Worker Configuration
# This is an alternative way to deploy using Wrangler CLI
# 
# Installation: npm install -g wrangler
# Deploy: wrangler deploy
# 
# Note: You can also deploy via the Cloudflare dashboard (see SETUP.md)

name = "pawamore-og-proxy"
main = "og-proxy-worker.js"
compatibility_date = "2024-01-01"

# Environment variables (set via dashboard or wrangler secret put)
# [vars]
# SUPABASE_URL = "https://caxlowsbpzjuegdwdqsi.supabase.co"
# APP_URL = "https://pawamore.lovable.app"

# SUPABASE_ANON_KEY should be set as a secret:
# wrangler secret put SUPABASE_ANON_KEY

# Custom domain (set via dashboard after first deploy)
# routes = [
#   { pattern = "share.pawamore.com/*", zone_name = "pawamore.com" }
# ]
